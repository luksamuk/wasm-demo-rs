[package]
name = "wasm-demo-rs"
version = "0.2.1"
authors = ["Lucas Vieira <lucasvieira@protonmail.com>"]
description = "A basic example of Rust being compiled to WebAssembly, using Rust's own native tools. Requires nightly toolchain."

[dependencies]
stdweb = "0.4.1"
